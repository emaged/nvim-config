No explicit dev commands documented in README beyond launching Neovim: `nvim`.
Tooling configs suggest optional usage of:
- `stylua <path>` for formatting Lua
- `selene <path>` for Lua linting
(Confirm exact commands/paths if you have a preferred workflow.)